import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "GrowthProAI - Mini Local Business Dashboard",
  description: "Discover your business insights and AI-powered SEO headlines with GrowthProAI",
  keywords: "business dashboard, SEO, local business, AI headlines, business analytics",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
